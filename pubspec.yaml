name: thankinh_pex
description: Ứng dụng theo dõi điều trị thay huyết tương (PEX)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  signature: ^5.4.0
   udp: ^5.0.2
  multicast_dns: ^0.3.0
  shared_preferences: ^2.2.2


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

